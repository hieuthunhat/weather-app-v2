import React from 'react'

/**
 * Header
 * Side bar (drawer) Main body
 *                  Analytics
 * @returns
 */
const AppLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default AppLayout